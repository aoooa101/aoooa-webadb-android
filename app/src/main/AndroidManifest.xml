<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        WebADB 控制台 - Android 客户端
        权限说明：
        - USB Host: 通过原生 USB 连接 ADB 设备（替代网页版 WebUSB，解锁系统级 USB 能力）
        - INTERNET: 无线调试 TCP 连接（IP:5555）与页面资源加载
        - ACCESS_NETWORK_STATE: 检测网络状态（无线连接提示）
        - NEARBY_WIFI_DEVICES: Android 13+ 局域网设备通信所需（neverForLocation）
        - POST_NOTIFICATIONS: Android 13+ 无线配对通知栏交互所需
        - FOREGROUND_SERVICE: 后台 mDNS 配对服务与通知栏交互
    -->
    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WebADB"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </activity>

        <!-- 无线配对后台 mDNS 发现服务 -->
        <service
            android:name=".pairing.PairingService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- 通知栏小飞机发送广播接收器 -->
        <receiver
            android:name=".pairing.PairingActionReceiver"
            android:exported="false" />

    </application>

</manifest>
